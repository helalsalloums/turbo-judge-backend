info:
  name: submit
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/submission/
  body:
    type: json
    data: |-
      {
        "code": "print('hello')",
        "language": "python",
        "problemId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
