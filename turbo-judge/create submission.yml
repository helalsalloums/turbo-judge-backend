info:
  name: create submission
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/problem/1
  body:
    type: json
    data: |-
      {
        "title": "Two Sum",
        "text": "Given an array of integers, return indices of the two numbers that add up to a target.",
        "difficulty": 1200,
        "topics": ["arrays", "hash-map"],
        "timeLimit": 1000,
        "memoryLimit": 256
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
